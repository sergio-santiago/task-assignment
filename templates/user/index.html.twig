{% extends 'base.html.twig' %}

{% block title %}Users List{% endblock %}

{% block body %}
    <h3>Users</h3>
    <ul>
        {% for user in users %}
            <li>USER {{ user.id }}: {{ user.username }} - {{ user.email }}</li>
        {% endfor %}
    </ul>
{% endblock %}
